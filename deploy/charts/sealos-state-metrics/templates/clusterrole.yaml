{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "sealos-state-metrics.fullname" . }}
  labels:
    {{- include "sealos-state-metrics.labels" . | nindent 4 }}
rules:
  # Core resources
  - apiGroups: [""]
    resources:
      - nodes
      - pods
      - events
      - namespaces
    verbs: ["get", "list", "watch"]

{{- if has "database" .Values.enabledCollectors }}
  # Database resources (for database collector)
  - apiGroups: [""]
    resources:
      - secrets
    verbs: ["get", "list", "watch"]
{{- end }}

  # Metrics resources
  - apiGroups: ["metrics.k8s.io"]
    resources:
      - nodes
      - pods
    verbs: ["get", "list"]

{{- if has "kubeblocks" .Values.enabledCollectors }}
  # KubeBlocks resources (for kubeblocks collector)
  - apiGroups: ["apps.kubeblocks.io"]
    resources:
      - clusters
    verbs: ["get", "list", "watch"]
{{- end }}

{{- if has "crd" .Values.enabledCollectors }}
  # CRD resources (for crd collector)
  - apiGroups: ["apiextensions.k8s.io"]
    resources:
      - customresourcedefinitions
    verbs: ["get", "list", "watch"]
  # Access to custom resources (for crd collector)
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["get", "list", "watch"]
{{- end }}

  # Coordination for leader election
  - apiGroups: ["coordination.k8s.io"]
    resources:
      - leases
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

{{- if .Values.auth.enabled }}
  # Authentication and authorization (required when auth is enabled)
  - apiGroups: ["authentication.k8s.io"]
    resources:
      - tokenreviews
    verbs: ["create"]
  - apiGroups: ["authorization.k8s.io"]
    resources:
      - subjectaccessreviews
    verbs: ["create"]
{{- end }}
{{- end }}
