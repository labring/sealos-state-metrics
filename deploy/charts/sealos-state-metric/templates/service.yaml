apiVersion: v1
kind: Service
metadata:
  name: {{ include "sealos-state-metric.fullname" . }}
  labels:
    {{- include "sealos-state-metric.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sealos-state-metric.selectorLabels" . | nindent 4 }}
